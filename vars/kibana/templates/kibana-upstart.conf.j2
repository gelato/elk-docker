description "Kibana container"
author "Mizeraj"
start on filesystem and started docker
stop on runlevel [!2345]
respawn
script
  /usr/bin/docker start -a kibana-{{ hostvars[inventory_hostname]['inventory_hostname'] }}
end script
